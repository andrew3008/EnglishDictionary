# M0 — baseline: без агента и extension (performance-test-matrix).
# Пара M0/M0 (два независимых прогона) калибрует шум хоста: Δ CPU > 1% => лаборатория непригодна.
SCENARIO_ID=m0
PERF_SCENARIO_JVM_OPTS=
