# 09 — Plan Redline Input

> Redline table for `drop-oldest_refactoring_plan_262d2bc7.plan.md`.
> Each row cites an actual phrase from the plan, identifies the problem, and provides replacement wording.
> Severity: CRITICAL (blocks merge) / HIGH (must resolve before principal sign-off) / MEDIUM (must resolve before PR-3) / LOW (advisory).

---

## Redline Table

| # | Current phrase in plan | Problem | Replacement | Severity |
|---|----------------------|---------|-------------|----------|
| 1 | "Целевая архитектура: V6-clean-lite … Это **target architecture, а не conditional future option**." (§1, Executive Summary) | States V6-clean-lite as a settled target before Phase 0–3 validation. No repository ADR formally approves the decomposition. Architects object that "target architecture" implies approval that has not been granted through the standard ADR process. | "V6-clean-lite is the **candidate target architecture subject to Phase 0–3 validation**. It becomes the confirmed target architecture upon: (a) PR-0 safety net green, (b) principal architect sign-off on the Phase 0 evidence, (c) ADR formally approved." | CRITICAL |
| 2 | "**Backward compatibility** для Builder/JMX **не является hard constraint** (pre-production)" (§1, Executive Summary) | Conflates two distinct categories: (a) internal implementation details freely changeable in pre-production mode; (b) operator-facing contracts (`PlatformExportProcessorFactory` wiring, JMX schema, Builder used by factory) that require a migration path even in pre-production because operators and automation depend on them. | "Internal implementation details (queue ownership, lock pattern, lifecycle state machine) are freely changeable in pre-production mode. **Operator-facing contracts** (factory `Builder` API, JMX attribute schema) distinguish from internal details: they require a **documented migration path** (updated factory wiring in the same PR; new JMX schema documented before activation)." | HIGH |
| 3 | "JMX сохраняется как adapter-слой … **JMX schema может быть переработана** под чистую snapshot-модель, старые attribute names не являются constraint" (§1 and §4) | "May be redesigned" without a migration path constraint. JMX is consumed by operators and monitoring infrastructure. An undocumented schema change breaks existing dashboards and alerts silently. The plan grants redesign permission without stating the minimum migration obligation. | "JMX capability is preserved as an adapter layer. **JMX schema may be redesigned only with a documented operator migration path**: (a) the new attribute-name-to-snapshot-field mapping is merged in the same PR as the schema change (PR-4); (b) the old attribute names are noted as discontinued in the migration doc; (c) SRE is notified before activation." | HIGH |
| 4 | "PR-3: clean shutdown/forceFlush semantics — bounded shutdown, deterministic forceFlush, никогда не оставлять CompletableResultCode незавершённым" (plan todos, id: pr3) and "clean lifecycle semantics" (§6 PR-3 heading) | "Clean lifecycle semantics" frames bounded shutdown and deterministic forceFlush as internal housekeeping. In fact, changing the shutdown timeout from unbounded to bounded and changing forceFlush completion guarantees during SHUTTING_DOWN are **observable semantic changes**: they alter what callers of `shutdown().join()` and `forceFlush().join()` observe. Hiding them as a "clean" refactoring delays the ADR decision. | "**Bounded shutdown and deterministic forceFlush are observable semantic changes requiring an ADR decision** before PR-3 merges. The ADR must document: (a) the specific observable delta vs current unbounded behavior; (b) who is notified (SDK lifecycle callers, SRE); (c) the rollback path if the bounded timeout proves too short in practice." | HIGH |
| 5 | "PR-4: … обновить factory wiring в том же PR" (plan todos, id: pr4) — factory changes listed without explicit rollback integration gate | PR-4 updates `PlatformExportProcessorFactory` but the acceptance criteria do not include an explicit test that verifies rollback (unsetting the opt-in env var routes to stock BSP). This gate is safety-critical: without it, a misconfigured deployment cannot roll back without a code change. | Add to PR-4 acceptance criteria: "**PR-4 must test**: (a) opt-in activation (`DROP_OLDEST` policy → custom processor); (b) default passthrough (no policy → stock BSP); (c) **rollback by unsetting `PLATFORM_TRACING_QUEUE_OVERFLOW_POLICY` routes to stock BSP, not custom processor**; (d) multi-exporter fallback; (e) no-double-export." | HIGH |
| 6 | "Logging throttle … можно перейти на rate-limited / structured logging" (§10, Migration Decisions table) | "Can transition" framed as an optional refactoring choice. Changing `logExportFailureOnce` behavior (from one-shot to rate-limited or structured) is an operator-observable change: it alters the number and format of WARN log entries, affects SRE alert counts, and may break monitoring rules. It is not a neutral cleanup. | "**`logExportFailureOnce` behavior change is operator-observable and requires SRE coordination**, not an autonomous refactoring decision. Transitioning from one-shot to rate-limited or structured logging must be: (a) decided by the platform team with SRE input; (b) documented in a named ADR section or PR; (c) deployed with advance SRE notification." | MEDIUM |
| 7 | "Если **M1/M2** показывают зависший `CompletableResultCode` — это фиксируется как **known failure** (issue/ADR ref). В V6-clean он будет закрыт в PR-3. **PR-1/PR-2 могут стартовать параллельно**" (§5 Phase 0 Failure Decision Policy) — no explicit merge blocker stated for PR-3 | The policy allows M1/M2 to be `@Disabled` indefinitely as "known failures." It does not state that M1 or M2 being `@Disabled` **blocks PR-3 from merging**. A team under schedule pressure could merge PR-3 with M1/M2 still disabled, claiming "PR-3 addresses R3" without a green test to prove it. | Add: "**PR-3 cannot merge while M1 or M2 are `@Disabled`.** M1 and M2 must be active and green at PR-3 merge point. A PR-3 that makes M1/M2 green is what constitutes successful clean-lifecycle delivery — not a code change that happens to compile." | CRITICAL |
| 8 | "~7–8 production-классов, которые ревьюер может понять за один проход" and "Это **target architecture, а не conditional future option**" — the count is presented as a design target | The numeric class count (~7–8) is presented alongside "target architecture" framing, giving it the appearance of a design KPI. Reviewers may interpret missing classes as incompleteness and overengineers may create speculative classes to reach the count. | "~7–8 production classes is an **anti-overengineering guardrail, not a numeric KPI**. The correct number of classes is the minimum that achieves clean single-responsibility boundaries per §7a. A design with 6 classes that eliminates all hazards is preferable to one with 8 that includes speculative scaffolding." | LOW |
| 9 | "**Backward compatibility** … **не является hard constraint**" (§1) AND "**Legacy getters (мягко):** facade в PR-2 **может временно сохранить** старые getter-методы" (§6 PR-2) | These two statements directly contradict each other. If backward compatibility is not required, why temporarily preserve the legacy getters? If legacy getters are temporarily preserved, backward compatibility is being maintained at PR-2 boundaries. The contradiction signals an unresolved design decision about the migration bridge strategy. | Choose one of two explicit positions: **(A) Migration bridge:** "Legacy getters are temporarily preserved in PR-2 as an explicit migration bridge. They are removed in PR-5 (not PR-4). The bridge is documented as intentional, not as a contradiction of the breaking-changes stance." OR **(B) Hard break:** "Legacy getters are removed in PR-4. Tests that depend on them are migrated in PR-4. No bridge is created. This requires migrating all callers in a single PR." State the chosen position in §1 and §6. | HIGH |
| 10 | "Целевая архитектура: V6-clean-lite … Статус: Proposed" (plan header) — "proposed" in header but "target architecture" in body | The plan header says "Proposed" but the body asserts V6-clean-lite as a confirmed target. The combination is incoherent: a proposed plan cannot simultaneously assert a target that is not conditional. Architects correctly identify this as overconfident framing. | Replace all "V6-clean-lite is the target architecture" assertions with: "V6-clean-lite is **approved as a candidate architecture**. **Final approval is conditional on Phase 0–3 validation**: green M1/M2/M10, no material JMH regression, and principal architect sign-off at the PR-2 review gate." The word "approved" should not appear in the plan body without a reference to the specific review that granted approval. | CRITICAL |

---

## Summary of Severity Distribution

| Severity | Count | Items |
|----------|-------|-------|
| CRITICAL | 3 | #1 (target architecture without ADR), #7 (M1/M2 not blocking PR-3), #10 (approved framing) |
| HIGH | 4 | #2 (backward compat conflation), #3 (JMX schema without migration path), #4 (semantic changes hidden as clean), #5 (PR-4 no rollback gate), #9 (contradiction) |
| MEDIUM | 1 | #6 (logExportFailureOnce) |
| LOW | 1 | #8 (class count as KPI) |

---

## Minimum Changes Required for Principal Architect Sign-Off

1. Replace all "target architecture" assertions with "candidate target architecture subject to Phase 0–3 validation."
2. Add the explicit statement: "PR-3 cannot merge while M1 or M2 are `@Disabled`."
3. Add PR-4 rollback test gate: unsetting policy routes to stock BSP.
4. Resolve the legacy-getter contradiction by choosing explicit migration bridge or hard break.
5. Add the ADR requirement for bounded shutdown as an observable semantic change.
